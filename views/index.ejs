<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Workshop Demo</title>
    <style>
      :root {
        --sky: #38bdf8;
        --sky-dark: #0ea5e9;
        --text-dark: #1e293b;
        --text-light: #ffffff;
        --accent: #0f172a;
        --button-hover: #0284c7;
      }

      * {
        box-sizing: border-box;
        font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
        margin: 0;
        padding: 0;
      }

      body {
        background: linear-gradient(
          to right,
          var(--text-light),
          var(--text-light)
        );
        color: var(--text-light);
        height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 2rem;
      }

      .hero {
        background: linear-gradient(to right, var(--sky), var(--sky-dark));
        padding: 3rem;
        border-radius: 1rem;
        max-width: 700px;
        text-align: center;
        box-shadow: 0 8px 20px rgba(0, 0, 0, 0.2);
      }

      .hero h1 {
        font-size: 2.5rem;
        margin-bottom: 1rem;
        line-height: 1.3;
      }

      .hero p {
        font-size: 1.1rem;
        margin-bottom: 2rem;
        color: #e0f2fe;
      }

      .hero .buttons {
        display: flex;
        justify-content: center;
        gap: 1.5rem;
        flex-wrap: wrap;
      }

      .hero a.button {
        text-decoration: none;
        background-color: var(--text-light);
        color: var(--accent);
        padding: 0.75rem 1.5rem;
        border-radius: 0.5rem;
        font-weight: bold;
        transition: background-color 0.3s, color 0.3s;
      }

      .hero a.button:hover {
        background-color: var(--button-hover);
        color: var(--text-light);
      }
    </style>
  </head>
  <body>
    <div class="hero">
      <h1>Navigating the Tech Industry<br />as a Beginner with AI</h1>
      <p>
        Explore how tools like ChatGPT and GitHub Copilot accelerate learning
        and development.
      </p>
      <div class="buttons">
        <a href="/students" class="button">View Students</a>
        <a href="/courses" class="button">View Courses</a>
      </div>
    </div>
  </body>
</html>
